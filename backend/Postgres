# Use the official PostgreSQL image as the base
FROM postgres:13

# Optional: Set environment variables (e.g., default user, password, database)
ENV POSTGRES_USER=myuser
ENV POSTGRES_PASSWORD=mypassword
ENV POSTGRES_DB=mydatabase
